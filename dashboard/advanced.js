(function(){
'use strict';
const q=id=>document.getElementById(id);
const esc=v=>String(v??'—').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const num=(v,d=1)=>Number.isFinite(Number(v))?Number(v).toFixed(d):'—';
const pct=v=>v==null?'—':num(v,1)+'%';
const clamp=(v,a=0,b=100)=>Math.max(a,Math.min(b,Number(v)||0));
function parseTs(ts){if(!ts)return NaN;let s=String(ts);if(!/[zZ]|[+-]\d\d:\d\d$/.test(s))s+='Z';return Date.parse(s.replace(' ','T'));}
function age(ts){const t=parseTs(ts);if(!Number.isFinite(t))return '—';const s=Math.max(0,(Date.now()-t)/1000);return s<60?s.toFixed(1)+'s':Math.floor(s/60)+'m';}
function event(){return window.D?.latest?.early_event||null;}
function send(action,reason){const e=event();return fetch('/api/action',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,reason,event_key:e?.event_key||''})}).then(r=>r.json());}
function metric(label,value,sub=''){return `<div class="ixmetric"><span>${esc(label)}</span><b>${esc(value)}</b><small>${esc(sub)}</small></div>`;}
function bar(label,value,max=100,suffix=''){const v=clamp(value,0,max),w=(v/max)*100;return `<div class="ibar"><div><span>${esc(label)}</span><b>${esc(num(value,1))}${esc(suffix)}</b></div><div class="itrack"><i style="width:${w}%"></i></div></div>`;}
function titleFor(k){return ({MOMENTUM:'Momentum Analysis',REVERSAL:'Reversal Analysis',TRAP:'Trap Detector',EXPLOSIVE:'Explosive Move Analysis',CEPE:'CE vs PE Comparison',ENTRY:'Entry Quality',TIMING:'Timing Replay',REASONING:'AI Evidence Chain',CONTEXT:'Market Context',DEEP:'Deep Event Dossier',OUTCOME:'Outcome Replay'})[k]||'Event Analysis';}
function baseEvent(){const e=event();if(!e)return `<div class="ixempty">No live event is selected. The console will populate when the observer records an event.</div>`;return `<div class="ixevent"><div><b>${esc(e.symbol)} ${esc(e.strike)} ${esc(e.option_type)}</b><small>Expiry ${esc(e.expiry)} • ₹${num(e.ltp,2)} • event age ${age(e.observed_ts)}</small></div><strong>${esc(e.score??'—')}</strong></div>`;}
function momentum(e){const a=Number(e.move_1m_pct)||0,b=Number(e.move_3m_pct)||0,c=Number(e.move_5m_pct)||0,v=Number(e.volume_ratio)||0,s=Number(e.spread_pct)||0;const strength=clamp(Math.max(a,b,c)*8);return baseEvent()+`<div class="ixgrid">${metric('1 minute',pct(a),'fast impulse')}${metric('3 minutes',pct(b),'confirmation window')}${metric('5 minutes',pct(c),'expansion window')}${metric('Volume',num(v,2)+'x','participation')}${metric('Spread',pct(s),'execution friction')}${metric('Score',e.score??'—','automated score')}</div><div class="ixsection"><h4>Momentum profile</h4>${bar('1m impulse',strength)}${bar('3m confirmation',clamp(b*8))}${bar('5m expansion',clamp(c*8))}${bar('Participation',clamp(v*40))}</div><div class="ixconclusion"><b>${a>=b*0.8&&a>0?'ACCELERATION PRESENT':'MOMENTUM NEEDS CONFIRMATION'}</b><br>${s>5?'The move has meaningful spread friction; treat raw percentage gain as lower-quality execution evidence.':'Price expansion is present without an extreme spread penalty.'}</div>`;}
function reversal(e){const a=Number(e.move_1m_pct)||0,b=Number(e.move_3m_pct)||0,c=Number(e.move_5m_pct)||0;const decay=c>0?clamp((1-Math.max(a,0)/Math.max(c,0.01))*100):0;return baseEvent()+`<div class="ixgrid">${metric('1m velocity',pct(a))}${metric('3m velocity',pct(b))}${metric('5m velocity',pct(c))}${metric('Velocity decay',num(decay,1)+'%','higher = cooling')}${metric('Direction',String(e.option_type||'—').toUpperCase())}${metric('Event age',age(e.observed_ts))}</div><div class="ixsection"><h4>Reversal / exhaustion evidence</h4>${bar('Momentum retained',100-decay)}${bar('Cooling pressure',decay)}${bar('Short-window impulse',clamp(a*8))}</div><div class="ixconclusion"><b>${decay>35?'REVERSAL RISK ELEVATED':decay>15?'COOLING — WAIT FOR CONFIRMATION':'CONTINUATION BIAS'}</b><br>This panel separates a fast burst from evidence that the burst is losing velocity.</div>`;}
function trap(e){const spread=Number(e.spread_pct)||0,vol=Number(e.volume_ratio)||0,score=Number(e.score)||0,m=Math.max(Number(e.move_1m_pct)||0,Number(e.move_3m_pct)||0,Number(e.move_5m_pct)||0);const trap=clamp(spread*6+(vol<1?25:0)+(score<65?25:0)+(m>0&&spread>7?20:0));return baseEvent()+`<div class="ixgrid">${metric('Spread risk',pct(spread))}${metric('Volume ratio',num(vol,2)+'x')}${metric('Move',pct(m),'largest window')}${metric('Score',score)}${metric('Trap index',num(trap,0)+'/100')}${metric('Gate posture','FAIL-CLOSED')}</div><div class="ixsection"><h4>Trap pressure</h4>${bar('Spread friction',clamp(spread*6))}${bar('Low participation',vol<1?70:20)}${bar('Score weakness',clamp(100-score))}${bar('Composite trap index',trap)}</div><div class="ixconclusion"><b>${trap>=65?'HIGH TRAP RISK':trap>=40?'CAUTION':'LOWER TRAP PRESSURE'}</b><br>Wide spread, weak participation and weak score are treated as quality penalties rather than being hidden behind the headline move.</div>`;}
function explosive(e){const vals=[Number(e.move_1m_pct)||0,Number(e.move_3m_pct)||0,Number(e.move_5m_pct)||0],peak=Math.max(...vals),acc=vals[0]-(vals[2]||0);return baseEvent()+`<div class="ixgrid">${metric('Peak move',pct(peak))}${metric('Acceleration delta',pct(acc))}${metric('1m',pct(vals[0]))}${metric('3m',pct(vals[1]))}${metric('5m',pct(vals[2]))}${metric('Age',age(e.observed_ts))}</div><div class="ixsection"><h4>Move expansion ladder</h4>${bar('1m',clamp(vals[0]*8))}${bar('3m',clamp(vals[1]*8))}${bar('5m',clamp(vals[2]*8))}</div><div class="ixconclusion"><b>${peak>=8?'EXPLOSIVE':'FAST MOVE'}</b><br>The ladder shows whether the move is expanding across windows or only appearing as a short-lived spike.</div>`;}
function cepe(e){const rows=(window.D?.recent?.early_events||[]).filter(x=>x.symbol===e.symbol&&String(x.strike)===String(e.strike)).slice(0,12);const ce=rows.filter(x=>String(x.option_type).toUpperCase()==='CE')[0],pe=rows.filter(x=>String(x.option_type).toUpperCase()==='PE')[0];const one=x=>x?Math.max(Number(x.move_1m_pct)||0,Number(x.move_3m_pct)||0,Number(x.move_5m_pct)||0):0;return `<div class="ixcompare"><div><h4>CE</h4>${metric('Move',pct(one(ce)))}${metric('Score',ce?.score??'—')}${metric('LTP',ce?'₹'+num(ce.ltp,2):'—')}</div><div><h4>PE</h4>${metric('Move',pct(one(pe)))}${metric('Score',pe?.score??'—')}${metric('LTP',pe?'₹'+num(pe.ltp,2):'—')}</div></div><div class="ixsection"><h4>Relative pressure</h4>${bar('CE pressure',clamp(one(ce)*8))}${bar('PE pressure',clamp(one(pe)*8))}</div><div class="ixconclusion"><b>${one(ce)>one(pe)?'CE LEADS':one(pe)>one(ce)?'PE LEADS':'NO CLEAR LEADER'}</b><br>Comparison uses the latest same-symbol/same-strike event evidence available to the dashboard.</div>`;}
function entry(e){const move=Math.max(Number(e.move_1m_pct)||0,Number(e.move_3m_pct)||0,Number(e.move_5m_pct)||0),spread=Number(e.spread_pct)||0,score=Number(e.score)||0,quality=clamp(score-spread*3+(Number(e.volume_ratio)||0)*8);return baseEvent()+`<div class="ixgrid">${metric('Signal score',score+'/100')}${metric('Move captured',pct(move))}${metric('Spread',pct(spread))}${metric('Volume',num(e.volume_ratio,2)+'x')}${metric('Quality index',num(quality,0)+'/100')}${metric('Event age',age(e.observed_ts))}</div><div class="ixsection"><h4>Entry-quality components</h4>${bar('Automated score',score)}${bar('Participation',clamp((Number(e.volume_ratio)||0)*40))}${bar('Execution quality',quality)}${bar('Spread penalty',clamp(spread*6))}</div><div class="ixconclusion"><b>${quality>=75?'HIGH QUALITY EVIDENCE':quality>=55?'MEDIUM QUALITY — CONFIRM':'LOW QUALITY — AVOID CHASING'}</b><br>This is an evidence-quality view, not an instruction to bypass the automated entry gates.</div>`;}
function timing(e){const ts=parseTs(e.observed_ts),now=Date.now(),ageSec=Number.isFinite(ts)?Math.max(0,(now-ts)/1000):NaN;return baseEvent()+`<div class="timeline"><div class="tl on"><b>1. Market observation</b><span>source snapshot / candle evidence</span></div><div class="tl on"><b>2. Surge detection</b><span>event generated • ${esc(ageSec.toFixed(1))}s event age</span></div><div class="tl on"><b>3. Event persistence</b><span>Tier-1 early event recorded</span></div><div class="tl"><b>4. Bridge pickup</b><span>requires fresh event within existing age gate</span></div><div class="tl"><b>5. Suggestion / paper entry</b><span>only after option + risk gates</span></div><div class="tl"><b>6. Monitoring / outcome</b><span>closed outcome feeds learning</span></div></div><div class="ixconclusion"><b>IMPORTANT TIMING STATUS</b><br>The dashboard currently has authoritative event-record time. Bridge/entry timestamps are shown only when instrumented data exists; it will never invent latency.</div>`;}
function reasoning(e){const spread=Number(e.spread_pct)||0,vol=Number(e.volume_ratio)||0,score=Number(e.score)||0,m1=Number(e.move_1m_pct)||0;const positives=[m1>0,'3m expansion '+(Number(e.move_3m_pct)||0)>0,'5m expansion '+(Number(e.move_5m_pct)||0)>0,vol>=1,score>=65].filter(Boolean).length;const risks=[spread>5,vol<1,score<65].filter(Boolean).length;return baseEvent()+`<div class="ixgrid">${metric('Evidence factors',positives+'/5','supporting')}${metric('Risk factors',risks+'/3','quality warnings')}${metric('Score',score+'/100')}${metric('Volume',num(vol,2)+'x')}${metric('Spread',pct(spread))}${metric('Gate','AUTOMATED')}</div><div class="ixsection"><h4>Evidence balance</h4>${bar('Supporting evidence',positives*20)}${bar('Risk evidence',risks*33.3)}</div><div class="ixconclusion"><b>${positives>risks?'EVIDENCE LEANS POSITIVE':'EVIDENCE IS CONFLICTED'}</b><br>Positive factors and quality risks are displayed separately so a large move cannot hide weak execution evidence.</div>`;}
function context(e){return baseEvent()+`<div class="ixgrid">${metric('Underlying',e.symbol)}${metric('Option',String(e.option_type||'—').toUpperCase())}${metric('Expiry',e.expiry)}${metric('Strike',e.strike)}${metric('Score',e.score)}${metric('Age',age(e.observed_ts))}</div><div class="ixsection"><h4>Context stack</h4><div class="contextrow"><b>Underlying</b><span>Tier-1 event: ${esc(e.symbol)}</span></div><div class="contextrow"><b>Option</b><span>${esc(e.option_type)} ${esc(e.strike)} @ ₹${num(e.ltp,2)}</span></div><div class="contextrow"><b>Momentum</b><span>${pct(e.move_1m_pct)} / ${pct(e.move_3m_pct)} / ${pct(e.move_5m_pct)}</span></div><div class="contextrow"><b>Quality</b><span>Volume ${num(e.volume_ratio,2)}x • Spread ${pct(e.spread_pct)}</span></div></div><div class="ixconclusion"><b>CONTEXT FIRST</b><br>Use this view before interpreting the option move in isolation.</div>`;}
function deep(e){return baseEvent()+momentum(e)+trap(e)+timing(e).replace(baseEvent(),'');}
function outcome(e){const outs=(window.D?.recent?.outcomes||[]).filter(o=>String(o.symbol||'')===String(e.symbol||''));return baseEvent()+`<div class="ixsection"><h4>Observed outcome history for ${esc(e.symbol)}</h4>${outs.length?outs.slice(0,8).map(o=>`<div class="contextrow"><b>${esc(o.ts)}</b><span>P&L ₹${num(o.pnl,2)} • ${esc(o.exit_reason)} • score ${esc(o.score)}</span></div>`).join(''):'<div class="ixempty">No matching closed outcomes in the dashboard dataset.</div>'}</div><div class="ixconclusion"><b>LEARNING VIEW</b><br>This panel reports recorded outcomes; it does not rewrite or reinterpret historical records.</div>`;}
const builders={MOMENTUM:momentum,REVERSAL:reversal,TRAP:trap,EXPLOSIVE:explosive,CEPE:cepe,ENTRY:entry,TIMING:timing,REASONING:reasoning,CONTEXT:context,DEEP:deep,OUTCOME:outcome};
function openAnalysis(k){const e=event();q('ixModalTitle').textContent=titleFor(k);q('ixModalBody').innerHTML=e?(builders[k]||reasoning)(e):'<div class="ixempty">No current event.</div>';q('ixModal').style.display='block';}
function closeAnalysis(){q('ixModal').style.display='none';}
function inject(){if(q('advancedCenter'))return;const grid=document.querySelector('.grid');if(!grid)return;const wrap=document.createElement('div');wrap.id='advancedCenter';wrap.className='card full';wrap.innerHTML=`<div class="h">⚡ ADVANCED LIVE INTELLIGENCE CONSOLE</div><div class="advhero"><div><div class="advtag">HIGH-VALUE TRADE SUGGESTION</div><div id="hvTitle" class="advtitle">Waiting for live evidence…</div><div id="hvMeta" class="muted">No qualifying event yet</div></div><div id="hvScore" class="advscore">—</div></div><div id="hvWhy" class="advwhy">Evidence-only. Automated option, market, session and risk gates remain authoritative.</div><div class="advgrid"><div class="advmetric"><b id="liveSurges">0</b><span>LIVE SURGES</span></div><div class="advmetric"><b id="avgCatch">—</b><span>EVENT AGE</span></div><div class="advmetric"><b id="bestCatch">—</b><span>FRESHEST</span></div><div class="advmetric"><b id="hvCount">0</b><span>HIGH SCORE</span></div></div><div class="h" style="margin-top:13px">🔬 EVENT INTERPRETATION — TAP TO OPEN REAL ANALYSIS</div><div class="ixbuttons">${[['MOMENTUM','🚀 Momentum'],['REVERSAL','🔄 Reversal'],['TRAP','🪤 Trap Detector'],['EXPLOSIVE','💥 Explosive Move'],['CEPE','📊 CE vs PE'],['ENTRY','🎯 Entry Quality'],['TIMING','⏱ Timing Replay'],['REASONING','🧠 AI Evidence'],['CONTEXT','🌐 Market Context'],['DEEP','🔬 Deep Dive'],['OUTCOME','👁 Outcome Replay']].map(x=>`<button class="ixbtn" data-ix="${x[0]}">${x[1]}</button>`).join('')}</div><div class="h" style="margin-top:13px">📡 LIVE SURGE EVENTS</div><div id="liveFeed" class="livefeed"></div></div>`;grid.insertBefore(wrap,grid.children[4]||null);document.body.insertAdjacentHTML('beforeend',`<div id="ixModal" class="ixmodal"><div class="ixbox"><button class="ixclose" onclick="window.closeIx()">✕</button><h2 id="ixModalTitle">Event Analysis</h2><div id="ixModalBody"></div></div></div>`);wrap.querySelectorAll('[data-ix]').forEach(b=>b.onclick=()=>openAnalysis(b.dataset.ix));window.closeIx=closeAnalysis;const oldSection=[...document.querySelectorAll('.card')].find(x=>x.textContent.includes('🧠 Manual Trade Interpretation'));if(oldSection){oldSection.style.display='none';}const st=document.createElement('style');st.textContent=`.advhero{display:flex;justify-content:space-between;gap:12px;padding:15px;border:1px solid #3a4f7c;border-radius:15px;background:linear-gradient(135deg,#14203a,#0d1525);box-shadow:0 8px 30px #0005}.advtag{font-size:10px;font-weight:900;letter-spacing:1px;color:#ffd166}.advtitle{font-size:23px;font-weight:950;margin:5px 0}.advscore{font-size:38px;font-weight:950}.advwhy{padding:10px 12px;margin-top:7px;background:#0c1526;border-radius:10px;color:#b9c5da;font-size:11px}.advgrid{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:7px}.advmetric{background:#101a2c;border:1px solid #293955;border-radius:11px;padding:10px;text-align:center}.advmetric b{display:block;font-size:19px}.advmetric span{font-size:9px;color:#91a0ba}.ixbuttons{display:grid;grid-template-columns:repeat(4,1fr);gap:7px}.ixbtn{background:#101a2c;color:#eef;border:1px solid #314467;border-radius:11px;padding:12px 8px;font-weight:800;cursor:pointer;min-height:54px}.ixbtn:hover{border-color:#8da4ff;transform:translateY(-1px)}.livefeed{display:grid;gap:6px;max-height:300px;overflow:auto}.liveitem{display:grid;grid-template-columns:1fr auto;gap:8px;padding:10px;background:#0d1729;border:1px solid #263753;border-radius:10px}.liveitem b{font-size:13px}.liveitem .lat{font-weight:900;font-size:15px}.liveitem small{display:block;color:#91a0ba;margin-top:3px}.hot{border-color:#8a6230}.ixmodal{display:none;position:fixed;inset:0;background:#000c;z-index:50;padding:10px;overflow:auto}.ixbox{max-width:820px;margin:3vh auto;background:#0d1627;border:1px solid #3b4f76;border-radius:17px;padding:15px;box-shadow:0 20px 80px #0008}.ixclose{float:right;background:none;border:0;color:#eef;font-size:25px;cursor:pointer}.ixbox h2{margin:3px 35px 13px 0}.ixevent{display:flex;justify-content:space-between;gap:10px;padding:12px;background:#111e34;border:1px solid #304463;border-radius:12px;margin-bottom:10px}.ixevent b{font-size:16px}.ixevent small{display:block;color:#91a0ba;margin-top:4px}.ixevent strong{font-size:25px}.ixgrid{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin:8px 0}.ixmetric{background:#111c30;border:1px solid #293b5a;border-radius:10px;padding:9px}.ixmetric span,.ixmetric small{display:block;color:#91a0ba;font-size:9px;text-transform:uppercase}.ixmetric b{display:block;font-size:17px;margin:4px 0}.ixsection{background:#0b1424;border:1px solid #263753;border-radius:12px;padding:11px;margin-top:8px}.ixsection h4{margin:0 0 9px;font-size:12px}.ibar{margin:9px 0}.ibar>div:first-child{display:flex;justify-content:space-between;font-size:10px;color:#aebbd1}.itrack{height:8px;background:#1b2940;border-radius:20px;overflow:hidden;margin-top:4px}.itrack i{display:block;height:100%;background:#7891ff;border-radius:20px}.ixconclusion{margin-top:8px;padding:11px;border-left:3px solid #6d8cff;background:#111c30;border-radius:8px;font-size:11px;line-height:1.55}.ixcompare{display:grid;grid-template-columns:1fr 1fr;gap:8px}.ixcompare>div{background:#101a2c;border:1px solid #293955;border-radius:12px;padding:10px}.ixcompare h4{margin:0 0 8px}.contextrow{display:flex;justify-content:space-between;gap:10px;padding:8px 0;border-bottom:1px solid #243149;font-size:11px}.contextrow:last-child{border:0}.contextrow span{color:#aebbd1;text-align:right}.ixempty{padding:20px;text-align:center;color:#91a0ba;background:#0b1424;border-radius:10px}@media(max-width:700px){.ixbuttons{grid-template-columns:repeat(2,1fr)}.ixgrid{grid-template-columns:repeat(2,1fr)}.advgrid{grid-template-columns:repeat(2,1fr)}.advtitle{font-size:18px}.advscore{font-size:30px}}@media(max-width:420px){.ixbuttons{grid-template-columns:1fr 1fr}.ixgrid{grid-template-columns:1fr 1fr}.ixbox{margin:1vh auto;padding:11px}}`;document.head.appendChild(st);}
function renderAdvanced(){inject();const d=window.D||{},es=d.recent?.early_events||[],active=es.filter(e=>{const t=parseTs(e.observed_ts);return Number.isFinite(t)&&(Date.now()-t)<=60000&&(Date.now()-t)>=0;}),scored=es.filter(e=>Number(e.score||0)>=80),hv=scored[0]||es[0];if(!q('liveFeed'))return;q('liveSurges').textContent=active.length;q('hvCount').textContent=scored.length;if(hv){q('hvTitle').textContent=`${hv.symbol||'—'} ${hv.strike||'—'} ${String(hv.option_type||'').toUpperCase()} • ₹${num(hv.ltp,2)}`;q('hvMeta').textContent=`Score ${hv.score??'—'} • 1m ${pct(hv.move_1m_pct)} • 3m ${pct(hv.move_3m_pct)} • 5m ${pct(hv.move_5m_pct)} • event ${age(hv.observed_ts)} ago`;q('hvScore').textContent=hv.score??'—';q('hvWhy').textContent=`Evidence: 1m/3m/5m ${pct(hv.move_1m_pct)} / ${pct(hv.move_3m_pct)} / ${pct(hv.move_5m_pct)} • volume ${num(hv.volume_ratio,2)}x • spread ${pct(hv.spread_pct)}. No automated gate is bypassed.`;}else{q('hvTitle').textContent='Waiting for live evidence…';q('hvMeta').textContent='No qualifying event yet';q('hvScore').textContent='—';}const ages=active.map(e=>(Date.now()-parseTs(e.observed_ts))/1000).filter(x=>x>=0);q('avgCatch').textContent=ages.length?(ages.reduce((a,b)=>a+b,0)/ages.length).toFixed(1)+'s':'—';q('bestCatch').textContent=ages.length?Math.min(...ages).toFixed(1)+'s':'—';q('liveFeed').innerHTML=active.slice(0,15).map(e=>{const l=(Date.now()-parseTs(e.observed_ts))/1000;return `<div class="liveitem ${Number(e.score||0)>=80?'hot':''}"><div><b>⚡ ${esc(e.symbol)} ${esc(e.strike)} ${esc(e.option_type)}</b><small>₹${num(e.ltp,2)} • score ${esc(e.score)} • 1m ${pct(e.move_1m_pct)} • 3m ${pct(e.move_3m_pct)} • vol ${num(e.volume_ratio,2)}x</small><small>Recorded ${esc(age(e.observed_ts))} ago • ${esc(e.event_key||e.id)}</small></div><div class="lat">${l.toFixed(1)}s</div></div>`}).join('')||'<div class="ixempty">No fresh surge event in the last 60 seconds.</div>';}
const oldRender=window.render;window.render=function(d){window.D=d;if(oldRender)oldRender(d);renderAdvanced();};const oldPoll=window.poll;window.poll=async function(){const r=await fetch('/api/state',{cache:'no-store'});const d=await r.json();window.D=d;window.render(d);};setInterval(()=>{try{renderAdvanced()}catch(e){}},2000);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',inject);else inject();
})();