(()=>{
const esc=v=>String(v??'—').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function pct(v){return v==null?'—':`${Number(v).toFixed(3)}%`}
function card(title,g){return `<div class="card"><div class="h">${esc(title)}</div><div class="scroll" style="max-height:360px">${g}</div></div>`}
function metrics(m){if(!m||!m.samples)return '<div class="muted">No qualifying samples</div>';return `<div class="kv"><span>Samples</span><b>${m.samples}</b></div><div class="kv"><span>Top quartile</span><b>${m.top_quartile??'—'}</b></div><div class="kv"><span>Raw top H15</span><b>${pct(m.raw_top_expected_pct)}</b></div><div class="kv"><span>Learned top H15</span><b>${pct(m.learned_top_expected_pct)}</b></div><div class="kv"><span>Lift</span><b>${pct(m.lift_pct)}</b></div><div class="kv"><span>H1 / H3 / H5</span><b>${pct(m.h1_expected_pct)} / ${pct(m.h3_expected_pct)} / ${pct(m.h5_expected_pct)}</b></div><div class="kv"><span>H10 / H15</span><b>${pct(m.h10_expected_pct)} / ${pct(m.h15_expected_pct)}</b></div><div class="kv"><span>Learned WIN</span><b>${pct((m.learned_top_win_rate??0)*100)}</b></div><div class="kv"><span>Learned STRONG</span><b>${pct((m.learned_top_strong_rate??0)*100)}</b></div><div class="kv"><span>Learned FALSE</span><b>${pct((m.learned_top_false_rate??0)*100)}</b></div>`}
function renderLearning(d){let l=d.learning||{},diag=l.shadow_diagnostics||{},o=diag.overall||{},sp=l.shadow_performance||{};let root=document.getElementById('perez-learning-detail');if(!root){root=document.createElement('div');root.id='perez-learning-detail';root.className='grid';let anchor=document.getElementById('counts')?.closest('.card');(anchor?.parentElement||document.querySelector('.grid')).insertBefore(root,anchor?.nextElementSibling||null)}let groups=(obj,title)=>{let rows=Object.entries(obj||{});if(!rows.length)return card(title,'<div class="muted">No group has the minimum 5 causal samples yet.</div>');return card(title,rows.map(([k,v])=>`<div class="event" style="margin-bottom:7px"><b>${esc(k)}</b>${metrics(v)}</div>`).join(''))};root.innerHTML=`<div class="card full"><div class="h">🧠 FULL LEARNING / SHADOW PROOF</div><div class="kv"><span>Causal / leakage-safe</span><b>${diag.leakage_safe?'YES':'CHECK'}</b></div><div class="kv"><span>Valid samples</span><b>${diag.total_valid??0}</b></div><div class="kv"><span>Shadow engine</span><b>${esc(sp.status||'—')}</b></div><div class="kv"><span>Shadow top-quartile samples</span><b>${sp.top_quartile??'—'}</b></div><div class="kv"><span>Raw → Learned expected</span><b>${pct(sp.raw_top_expected_pct)} → ${pct(sp.learned_top_expected_pct)}</b></div><div class="kv"><span>Measured lift</span><b>${pct(sp.lift_pct)}</b></div><div class="sub" style="margin-top:8px">Learning is advisory only. It cannot bypass score, option, market-data, session, affordability, or risk gates.</div></div>${card('Overall',metrics(o))}${groups(diag.by_symbol_option,'Symbol + CE/PE')}${groups(diag.by_score_bucket,'Score Buckets')}${groups(diag.by_symbol,'Symbols')}${groups(diag.by_option_type,'Option Types')}`}
window.addEventListener('perez:state',e=>renderLearning(e.detail));window.perezLearningRender=renderLearning;
})();